module.exports = {
  rules: {
    'quotes': ['error', 'single'], // Enforce single quotes
    'prettier/prettier': ['error', { 'singleQuote': true, 'endOfLine': 'auto' }] // Fix prettier conflicts
  }
};
